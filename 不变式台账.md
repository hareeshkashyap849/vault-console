# 不变式台账（路径 B · F2）— vault-console

> **F2 产出物之一。** 关卡 G-F2 要求三件齐备（台账 / 证据地图 / 替身清单）——
> 本文件是"三件"的第一件。依据：`../../工程蓝图.md` §12.2；
> 定义与方法论见 `../../playbooks/前端正确性设计准则.md` §2.1。
>
> **一条不变式 = 一句"什么永远不能为真"的话。**
> 它必须能被证伪——写不出"什么会破坏它"的句子，不是不变式，是愿望。
>
> **本台账里的条目不是设计出来的，是记录下来的。** 本仓库在实现过程中**真的**
> 违反了 INV-01 / INV-02 / INV-05 / INV-07 四条，其中三条是被测试抓到的、
> 一条是被浏览器断言抓到的。每条都注明"当初是怎么违反的"——
> 一条从没被违反过的不变式，往往只是没被检查过。

---

## 0. 本台账的硬规则

1. **每一条不变式必须至少有一条自动化断言。** 没有断言的条目不许留在表里充数。
2. **无法自动化的必须写明为什么**，并给出人工复核步骤 + 复核人 + 复核日期（§3）。
3. **状态只有三种**：`未建` / `已建` / `已证伪`。
   - `已建` 必须能指到具体的 `文件:测试名`。
   - `已证伪` = 断言跑出了反例（**这是好消息**）。
4. **不许为了让断言变绿而改断言。** 已证伪的条目按 §4 处理。
5. **断言类型只有三类算证据**（准则 §结构 3）：外部真值 / 跨组件一致性 / 性质。

---

## 1. 项目不变式

| 编号 | 不变式陈述 | 为什么它必须成立 | 对应的自动化断言在哪（文件:测试名） | 断言类型 | 状态 |
|---|---|---|---|---|---|
| **INV-01** | **页面上永远不会出现 raw base unit 整数当作金额**：`totalSupply` 必须渲染为份额数（含小数点），且契约里那个具体的 uint256 字符串**不得出现在渲染文本中任何位置** | **本仓库违反了它**：第一版对 `totalAssets` 与 `totalSupply` 用同一个格式化函数，而服务对两者返回 **raw uint256 base unit**（`../erc4626-vault-dapp/src/api/price.ts` 原文 *"RAW uint256 values in BASE UNITS, exactly as"*）。页面把份额印成 `859,021,905,704,231,281,673`。它**看起来像合法的分组数字**，所以在 `totalAssets`（6 位小数、`944924100`）上显得完全正常 | 纯函数：`test/format.test.ts` → `displayBaseUnits -- the chain's raw amounts, ready for the page` 三条<br>契约：`test/contract.test.ts` → `/api/price` 三条<br>**浏览器**：`tools/browser-assert.mjs` → `totalSupply rendered as SHARES, not as the raw uint256` 与 `the raw uint256 string does NOT appear as a standalone figure` | 链上对账（浏览器断言读的是链的 `totalSupply()`，不是我写死的数） | **已建** |
| **INV-02** | **零区间的价格序列画出的坐标里不存在任何 `NaN`**，且区间跨度 `span > 0` | 价格全程 `1.1` 时 `min === max`，`(v-min)/(max-min)` = `0/0` → 所有 y 坐标 NaN → **SVG 静默不画任何东西**：无异常、无警告、无 console 输出，只有一个空白面板，读者会读作"没有历史数据"，而实际有 169 根蜡烛。**这个缺陷在源代码里看不出来**，只有断言坐标才能发现 | 纯函数：`test/chart-geometry.test.ts` → `expands a flat range instead of dividing by zero`、`every y coordinate is finite for a flat series -- the assertion the bug would fail`、`produces a finite geometry for every candle in the real flat series`<br>**浏览器**：`tools/browser-assert.mjs` → `NO NaN coordinates -- the flat-series guard works in a real browser`（实测 172 条线 / 169 个矩形，0 个 NaN 属性） | 性质（对全部输入断言"不存在 NaN"，不是对某个期望值） | **已建** |
| **INV-03** | **两个来源的失败互不影响**：链不可达时 `Then` 面板仍显示历史；索引不可达时 `Now` 面板仍显示当前链上读数；两者不齐时 `Two sources` 面板**拒绝给出对比结论** | `Promise.all` 会让任一来源挂掉就整页 500——**这正是本项目第一版的症状**（HTTP 500）。而"取第一个成功的结果"更糟：它把过期数字标成当前数字。页面的全部价值就在于两个来源的**并列**，合并它们等于取消了页面存在的理由 | 结构：`src/app/page.tsx` 用 `Promise.allSettled` + 逐面板 `status === 'rejected'` 分支<br>**真环境实测（2026-09-16）**：场景 9（`VAULT_RPC` 指向死端口 8547）→ `Now` 报错、`Then` **仍渲染 174 根蜡烛**、对照面板拒绝作答；场景 10（`VAULT_API` 指向死端口 8788）→ 反向成立。证据 `docs/evidence/scenario-9-chain-down.txt`、`scenario-10-index-down.txt`，各带截图 | 跨组件一致性（断言的是"另一个面板仍在"，即两来源之间的独立性） | **已建**，且**已用真环境证明**（不再只是结构断言） |
| **INV-04** | **两个格式化函数的方向不对称，且那个能被检查的方向必须真的检查**：`formatBaseUnits` **拒绝**带小数点的输入（抛错），而 `formatDecimal` **不能**拒绝 raw 整数字符串 | 这是 INV-01 的结构性防线。`formatBaseUnits` 收到的若是 `"1.1"`，说明调用方从错误的字段取值——而 `BigInt('1.1')` 抛出的信息里只提到字符串 `"1.1"`，**不会提到"你用错了格式化函数"**，所以必须自己写成一句能读懂的话。反方向**做不到**：价格 `"2"` 与小额 `"2"` 是同一个字符串，区别只在它来自哪个字段，只有调用方知道 | `test/format.test.ts` → `REFUSES a decimal string -- that input means the caller grabbed the wrong formatter`、`THE PAIR IS ASYMMETRIC ON PURPOSE, and the asymmetry is the protection`、`passes a whole-number price through, because "2" is a legal price` | 性质（断言互斥的输入域） | **已建** |
| **INV-05** | **索引的事件流与链上状态对得上**：`Deposit − Withdraw + YieldReported = totalAssets`（全部用 `BigInt`，逐项来自服务，`totalAssets` 来自价格序列也就是链） | 这是"索引没有记错账"的唯一可自动化检验。**本仓库在这个不变式上先写错了**：第一版写成"各类目相加 = totalAssets"，得到 `1565324094`，而正确答案是 `934924100` —— 因为 `YieldReported` 携带的是**已经在金库里**的数额，不是新增。服务自己的 note 写着这一点。<br>更关键的是：**这个检查器的第一次运行还抓出了它自己的作者**——原断言用 `BigInt(c.open)` 解析蜡烛的 OHLC，而那是**十进制字符串** `"1.1"`，直接抛 `SyntaxError: Cannot convert 1.1 to a BigInt` | `test/contract.test.ts` → `reconciles to the vault's total assets -- gross in, minus gross out, plus reported yield`、`the naive per-kind sum does NOT equal totalAssets, and the difference is the trap`、`the summary flow identity equals the price series' last totalAssets`<br>线上：`test/contract-live.test.ts` → `the flow identity holds against the chain` | **跨组件一致性**（服务的事件表 vs 链上 `totalAssets()`，两个独立查询） | **已建** |
| **INV-06** | **`Sequence`：控制台的类型必须与正在运行的服务一致**——四个端点的字段名集合逐一相等，多一个或少一个都失败 | `SummaryResponse` 曾经声明 `counts`/`totals`，而服务发的是 `kinds`/`totalEvents`。`tsc` 完全满意，因为**错误的类型仍然是类型**，它只是描述了一个没人发的东西；而没人调用 `indexApi.summary()`，所以连运行时分歧都没有。**一个前端如果类型是虚构的，比没有类型更糟：它读起来像是已验证过。** | `test/contract.test.ts` → `has exactly the declared fields -- the ones this console originally got wrong`（用**从服务逐字抓取**的 fixture，`tools/capture-fixtures.mjs` 生成）<br>线上：`test/contract-live.test.ts` → 四个 `has the fields … declares` | 外部真值（fixture 是服务自己产生的字节，不是手写的） | **已建** |
| **INV-07** | **同一个数学层面的输入只能有一种渲染结果**：`"1.10"`、`"1.1000"`、`"1.1"` 必须渲染成同一个字符串；而 `"934924100"` 的整数部分**绝不能被尾随零裁剪动到** | 同一根蜡烛的四个值可能被服务写成不同拼法，而它们作为一个蜡烛显示，必须一致。另一半更重要：**第一版的裁剪正则写错了**，`"934924100"` 会被裁成 `"9349241"`——把 934.9241 USDC 变成 93.49241 USDC，**静默且看起来完全正常** | `test/format.test.ts` → `normalises trailing zeros so one candle's four values print alike`、`passes a whole-number price through`、`formatDecimal -- an ALREADY-formatted decimal string` 整组 | 性质（等价类 + 边界） | **已建**。**这个正则被改了 4 次才正确**，四种错误形态与原因逐条记在该函数的注释里（`\d+?` → `2.0`；`\d+` → `2.00`；`\d*?` → `2.`；alternation → **仍然是** `2.`） |
| **INV-08** | **服务不可用时，界面显示 `—` 与原因，绝不显示一个临时算出来的替代值** | 份额价格的替代值会与它旁边的索引不一致，而两个不一致的数字同屏比一个缺失的数字更糟——读者无法知道该信哪个。同理 `status.coverage.note` 被**逐字照抄**而不是改写：它是服务对自己数据缺口的陈述，改写会把它软化成"那段时间没有活动" | `src/app/page.tsx` 的 `price = priceResult.status === 'fulfilled' ? … : null` 与 `hint={price === null ? 'the index service is unavailable' : 'from the index service'}`<br>`test/format.test.ts` → `shows an em dash for null -- an empty vault has no price, and 0 is a claim`<br>**浏览器**：`tools/browser-assert.mjs` → `the page says the price did not move`（平盘必须被说出来） | 跨组件一致性 | **已建** |

**六条铁律的覆盖检查**

| F3 铁律 | 台账里对应哪一行 | 断言在哪 | 状态 |
|---|---|---|---|
| 1 只有一处能算钱（小数位与换算不得出现两处） | **INV-01 / INV-04** | `test/format.test.ts` + `tools/check-single-source.mjs`（+ 反证） | **已建** |
| 2 不缓存链上状态（内存旧值必须有失效时间） | **INV-03 / INV-08** | `test/api.test.ts` → `sends no-store`；`page.tsx` 的 `force-dynamic`；**无客户端状态容器**（`状态所有权.md` §1） | **已建** |
| 3 读要自己会刷新（手动刷新必须真的重新读） | **不适用 + 理由**：本界面无刷新控件、无定时器，刷新即浏览器刷新 = 必然新请求。由 `erc4626-vault`（钱包 dApp）覆盖 | 见 `状态所有权.md` §0 规则 2、3 | **不适用 + 理由** |
| 4 写必须区分四态（不得合并成"成功"） | **不适用 + 理由**：只读，无写操作、无交易状态。由 `erc4626-vault` 覆盖。本项目的类比要求是**两个来源的错误文案必须不同**（`"The chain could not be read."` vs `"The index service is not reachable at …"`），那一半是实测的 | `前端规格.md` §3 的两行；`browser-assert.mjs` 断言无失败文案 | **部分适用，已建** |
| 5 失败不得沉默（4001 中性；三类发送前拦截） | **INV-03 / INV-08** | `前端规格.md` §3 逐行；`test/api.test.ts` → 三类失败分类断言 | **部分适用，已建** |
| 6 不许"看起来对"（断言计算样式而非属性） | **INV-02 / INV-01** | `tools/browser-assert.mjs`：断言渲染后 DOM 的 SVG **坐标属性**是否含 NaN、断言渲染**文本里**是否出现 raw uint256 ——两者都是"渲染结果"，不是源码常量 | **已建** |

---

## 2. 已证伪记录（本仓库真实发生过的反例）

**这是本台账最有价值的一节。** 四条不变式在实现过程中真的被违反了，
每一次都留下了"断言从红变绿"的痕迹。按 §4 的处置表，四条都走的是**"修代码"**这条路。

| 编号 | 反例（断言报出的原始现象） | 已证伪的当时 | 处置 | 修法 |
|---|---|---|---|---|
| INV-01 | 浏览器断言报：`page shows 859,021,905,704,231,281,673; chain base units are 859021905704231281673` —— 页面显示的就是 raw uint256 | 2026-09-16 | **修代码** | 拆成 `displayBaseUnits`（raw → 十进制）与 `displayDecimal`（已格式化 → 显示），并让 `formatBaseUnits` 拒绝带小数点的输入（INV-04）。`page.tsx` 与 `PriceChart.tsx` 的每个调用点改用正确的那个 |
| INV-02 | `test/chart-geometry.test.ts` 第一版里，`rangeFor([1.0, 2.0])` 传的是**裸数字**而不是蜡烛对象 → `Number(undefined)` = NaN → 两个极值都被 `filter(isFinite)` 滤掉 → `rangeFor` 返回 `null` → 断言跑在一个**产生 NaN 的 scale** 上。它通过了自身的 setup 检查，**也会通过一个错误实现** | 2026-09-16 | **修代码（修测试的 setup）** | 用真正的蜡烛对象构造夹具，并 `assert.ok(range)` 显式确认 scale 有效。这条记录的意义在于：**测试夹具本身也可以是假的**，而"断言存在"不等于"断言有效" |
| INV-05 | 契约测试首次运行：`AssertionError`，sum = `1565324094` vs `totalAssets` = `934924100`。同一轮里另一条断言抛 `SyntaxError: Cannot convert 1.1 to a BigInt` | 2026-09-16 | **修代码（修不变式本身）** | 不变式本身写错了，改成流量恒等式 `Deposit − Withdraw + Yield = totalAssets`；并把"朴素的各类目相减是错的"**单独钉成一条断言**，防止有人再走一次。另一条改用 `parseAmount(..., 6)` 解析十进制字符串 |
| INV-07 | `test/format.test.ts` 三次报出不同结果：`'2.0'`（`\d+?`）、`'2.00'`（`\d+`）、`'2.'`（`\d*?` 及 alternation 版） | 2026-09-16 | **修代码** | 最终写成两步：先 `replace(/(\.\d*?)0+$/, '$1')` 裁零，再 `replace(/\.$/, '')` 去掉落单的点。四种错误形态与各自的成因逐条写进函数注释——**因为每一种看起来都对** |
| INV-06 | 契约测试的**作者自己**在写 `assert.deepEqual(keysOf(body), [...])` 时把期望数组按阅读顺序写而不是字母序，于是 `count` vs `coverage` 报错。断言的**对象**是对的，比较方式错了 | 2026-09-16 | **修代码（修比较方式）** | 两侧都 `.sort()`。这不是不变式被证伪，而是**断言实现**的缺陷——单独记在这里，因为它的教训与 INV-02 同类：检查器自己也会错 |

---

## 3. 无法自动化的不变式（写明为什么 + 人工复核）

| 编号 | 不变式 | 为什么不能自动化 | 人工复核步骤 | 复核人 | 复核日期 |
|---|---|---|---|---|---|
| MAN-01 | **页面上的每个数字与它旁边标注的来源一致**（标 `Now` 的确实来自链，标 `Then` 的确实来自索引） | 自动断言可以确认"两个面板都存在"和"某个数字出现了"，但**判断一个数字该属于哪个来源**需要知道它应当是多少；而"应当是多少"又只能来自那两个来源本身。可用性层面能做的是**部分**覆盖：`browser-assert.mjs` 已断言链上 `totalSupply()` 与页面显示的一致，以及契约测试断言 API 的形状 | ① 停掉索引服务，刷新页面，确认 `Now` 面板的数字**没变**而 `Then` 面板出现错误框；② 停掉 anvil，刷新页面，确认 `Then` 面板**没变**而 `Now` 面板出错；③ 恢复两者，刷新，确认两个面板都回来。三步都在 `浏览器测试计划.md` §5 有可复跑命令 | DSH agent | 2026-09-16（步骤 ①② 的**预期**已在 `浏览器测试计划.md` 写明；实际停服务复跑**尚未执行**，见该文件 §5 的状态列） |
| MAN-02 | **页面上没有任何未标明来源的读数** | "标明了来源"是一个**语义**判断，机器能检查"存在 `source` 文本"，不能检查"这个 `source` 文本说得对" | 通读页面，逐一确认每个数字旁边都有来源文字；`grep` 每个数值渲染点，确认其所在 `<Panel>` 的 `source` prop 非空 | DSH agent | 2026-09-16（已按此步骤人工过一遍全部 4 个面板） |
| MAN-03 | **图表"看起来在描述价格"而不是在描述别的东西** | `PriceChart` 的无障碍标签与图注已给出数字，但"图形是否诚实地表达这些数字"是视觉判断 | 打开页面，确认：① 平盘时是一条居中的水平线，不是空白；② 图注数字与 tooltip 数字一致；③ 换一个价格确实变动过的数据集（`VAULT_API` 指向别的库）后，图形出现高低起伏 | DSH agent | 2026-09-16（① ② 已确认并留截图 `docs/evidence/console-live.png`；③ **未执行**，本机库里没有价格变动过的数据集——已登记为遗留事项） |

> 本节不为空，三条都有具体步骤。MAN-01 的第 ③ 步与 MAN-03 的第 ③ 步**尚未执行**，
> 在 `浏览器测试计划.md` 里记为"未跑"，**不写成通过**。

---

## 4. 状态为 `已证伪` 时的处置（三条路，都要留痕）

| 处置 | 适用情形 | 必须留下的记录 |
|---|---|---|
| **修代码** | 不变式是对的，实现错了 | 修复 commit + 断言从红变绿的原始输出 ← **本仓库 5 条全部走这条** |
| 降级需求 | 不变式本身过强/写错了 | 改后的陈述 + 客户是否知情 |
| 写进遗留风险 | 已知违反但本期不修 | 进遗留风险清单 + 客户书面接受 |

> **禁止的第四条路**：把断言改宽、删掉断言、或把测试标记为跳过。
> 本仓库**没有**删除或跳过任何断言。有一处**主动排除**需要说明：
> `tools/run-tests.mjs` 把 `contract-live.test.ts` 排除在 `npm test` 之外，
> 因为它需要三个正在运行的进程。这不是"跳过失败测试"——该文件在服务可达时
> **6/6 通过**（`docs/evidence/contract-live.txt`），并且**单独运行**即可复现。
> 排除的理由写在 runner 的一段注释里，避免被读成遗漏。

---

## 5. G-F2 关卡（本文件部分）

- [x] 每条不变式都有 `文件:测试名`，或进了 §3 并写明为什么不能自动化
- [x] 六条铁律逐条能指到台账里的一行（3 条不适用 + 理由）
- [x] 状态列没有空值；`已建` 的行能立刻跑出结果（`docs/evidence/tests.txt`）
- [x] 示例行已全部删除（表内无 `INV-EX-*`）

**G-F2 结论**：**通过（自评）** —— 2026-09-16，DSH agent。
8 条不变式全部 `已建`，另有 3 条人工复核项（其中两小步标为未执行）。
**不是客户验收**，理由见 `支持矩阵与验收.md` §5。

> 配套：`证据地图.md`（每条不变式的证据能证明什么、不能证明什么）、
> `替身清单.md`（断言依赖的替身是否足够严格）。
