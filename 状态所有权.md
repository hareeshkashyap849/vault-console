# 状态所有权（路径 B · F2 / F3）— vault-console

> **F2 产出物之一，同时是 G-F3"无第二事实来源"的检查对象。**
> 关卡 G-F2 的判据原文：**"每个状态归谁所有"逐项有答案**（`../../工程蓝图.md` §12.2）。
>
> 起因是一个必然出错的结构：**同一个事实存在两个地方，其中一个必然会过期，
> 而你没写同步代码**（准则 §结构 1）。
>
> **本项目的形态与模板假设不同，先说清楚**：模板面向的是**可写 dApp**，
> 有 allowance、有交易四态、有本地缓存。本界面**只读**，所以：
> - 没有 allowance、没有交易状态、没有"发送前拦截"；
> - 但它有**更强制的一条**：`dynamic = 'force-dynamic'` + `cache: 'no-store'`，
>   即**界面读数在任何情况下都不缓存**。这不是选择，是页面正确性的前提——
>   一个缓存过的控制台会把"一分钟前为真"的数字当作"现在为真"来展示。
>
> 逐行表里凡是"不适用"都给了理由；不适用 ≠ 不填。

---

## 0. 三条硬规则（逐条对照本项目）

1. **不缓存链上状态。** 界面读数每次从链或 API 取。确实必须留在内存里的旧值，
   **必须有明确的失效时间（TTL）**，且界面上要能看出它可能过期。

   **本项目的做法比"TTL"更硬**：**没有任何 TTL**，因为根本不留。
   - `src/app/page.tsx` 导出 `dynamic = 'force-dynamic'` 与 `revalidate = 0`；
   - `src/lib/api.ts` 的 `LIVE = { cache: 'no-store' }` 施加在**每一个** `fetch` 上；
   - 页面**没有**任何客户端定时器、没有轮询、没有 SWR/React Query 缓存。
     刷新 = 浏览器刷新 = 一次全新的服务端取数。
   - 有断言：`test/api.test.ts` → `sends no-store, because a cached console shows stale figures as current`。

   **为什么不做定时轮询**：本项目第一版（姊妹项目 `erc4626-vault` 的 K 线图）
   做过 `setInterval` 15 秒轮询，而准则 §0 规则 3 记录的正是这类结构的真实缺陷
   （读定时器与倒计时定时器互相饿死）。控制台是**用来看当前状态**的页面，
   服务端取数 + 显式刷新已经足够，且**没有可失效的缓存**。

2. **手动刷新控件必须执行一次真实的重读。**
   **不适用 + 理由**：本界面**没有**"刷新"按钮——刷新就是浏览器自身的刷新，
   它必然发起新的 HTTP 请求，因此不存在"按钮只重置计时器却没重读"这个缺陷的任何入口。
   **覆盖情况**：`erc4626-vault`（钱包 dApp）有真实刷新控件，那里有对应的
   "手动刷新必须产生一次新读取"的断言。

3. **定时刷新不得被自己的 UI 倒计时饿死。**
   **不适用 + 理由**：本界面**没有任何定时器**（见规则 1）。没有定时器就没有饿死。
   **覆盖情况**：`erc4626-vault` 的 K 线图有唯一一个 `setInterval`，与渲染逻辑分离，
   并且 `stopLive()` 会清掉它；那里有"在 N 秒内至少发生一次真实读取"的断言。

> 这三条在模板里是"必须做的三件事"。本项目**三条都不适用**，
> 但理由各不相同：规则 1 是**用更强的手段满足**（不缓存，而不是缓存+TTL），
> 规则 2、3 是**结构上不存在**。这三种情况的区别写在这里，
> 免得后来的人把"不适用"读成"没做"。

---

## 1. 状态所有权表（逐行填实）

**权威来源**只填三种：`链` / `API` / `内存声明式 UI 状态`。

| 状态 | 权威来源（链 / API / 内存声明式 UI 状态） | dApp 是否可缓存 | 缓存失效条件 | 谁负责刷新 | 刷新触发方式（手动按钮 / 定时 / 事件） |
|---|---|---|---|---|---|
| 金库地址 `vault` | **内存声明式 UI 状态**（启动时从部署记录文件读入，整个进程生命周期不变） | **是，且刻意永久** | 不失效。它来自 `deployments/local.json`，**重新部署才会变**，而重新部署就要重启控制台 | `src/lib/deployment.ts` 的 `loadDeployment()`，在 `page.tsx` 里**每次请求都调用**（不做模块级缓存，见下方说明） | 事件（进程生命周期：每次请求重新读文件） |
| 资产地址 `asset` | 同上 | 同上 | 同上 | 同上 | 同上 |
| `deployment.chainId` / `chainName` / `recordPath` | 同上 | 同上 | 同上 | 同上 | 同上 |
| `totalAssets` | **链**（`totalAssets()`，`src/lib/chain.ts`） | **否** | 不适用（每次请求重读，无副本留存） | `readDeployment()` 的调用方（`page.tsx`），**不是**任何组件 | 事件（每个 HTTP 请求一次 `Promise.allSettled`） |
| `totalSupply` | **链**（`totalSupply()`） | **否** | 同上 | 同上 | 同上 |
| `assetDecimals` | **链**（ERC-20 `decimals()`） | **否** | 同上 | 同上 | 同上 |
| `shareDecimals` | **链**（金库 `decimals()`） | **否** | 同上 | 同上 | 同上 |
| `assetSymbol` | **链**（ERC-20 `symbol()`） | **否** | 同上 | 同上 | 同上 |
| 份额价格 `price` | **API**（`/api/price?limit=1` 的 `series.at(-1).price`） | **否** | 不适用。**且刻意不在页面重算**——见 §2 的第二条 | `indexApi.price(1)` 的调用方 | 事件（每个 HTTP 请求一次） |
| 蜡烛序列 `candles` | **API**（`/api/candles?bucket=60&limit=5000`） | **否** | 不适用 | `indexApi.candles(60, 5000)` 的调用方 | 事件（每个 HTTP 请求一次） |
| 索引健康 `status`（含 `lagBlocks` / `staleSeconds` / `eventCount` / `snapshotCount` / `coverage`） | **API**（`/api/status`） | **否** | 不适用 | `indexApi.status()` 的调用方 | 事件（每个 HTTP 请求一次） |
| 链路失败 `liveResult.reason` | **内存声明式 UI 状态**（`Promise.allSettled` 的一条结果） | **否** | 不适用（与本次请求同生命周期，请求结束即消失） | `page.tsx` 的 `Promise.allSettled` | 事件（每次请求重新 settle） |
| 索引失败 `statusResult` / `candleResult` / `priceResult.reason` | 同上，**各自独立一条** | **否** | 同上 | 同上 | 同上 |
| 图表几何（`range` / `scale` / `cols`） | **内存声明式 UI 状态**（`PriceChart` 内的 `useMemo`，**从 props 派生**） | **是，TTL = 该组 props 的生命周期** | 当 `candles` 数组引用变化时由 React 重算；组件卸载即消失 | React 的 `useMemo`（依赖数组 `[candles]`） | 事件（props 变化） |
| 可访问的链条目 `candles[i].startsAt`（用作 React `key`） | **API**（服务保证桶对齐 epoch，故同一桶在不同查询里 `startsAt` 相同） | 不适用（是渲染 key，不是状态） | — | — | — |

**说明：为什么 `loadDeployment()` 每个请求都调用，而不是在模块顶层调一次**

模块顶层的 `const DEPLOYMENT = loadDeployment()` 会把它变成一个**进程级缓存**，
而"重新部署 → 重启进程"是一个**假设**，不是保证（`next start` 的进程可能比部署活得久）。
每次请求读一个几百字节的 JSON 的代价可以忽略，换来的是"页头显示的地址一定是这个请求读到的地址"。
这是"宁可多读一次文件，不要有一个会过期的副本"的一个具体例子。

---

## 2. 两处刻意的"不做缓存、也不做重算"

### 2.1 份额价格不在页面里重算

页面**曾经**从 `totalAssets` 与 `totalSupply` 自己算份额价格。这是**第二事实来源**：
服务在 `../erc4626-vault-dapp/src/api/price.ts` 的 `sharePrice()` 里实现并测试了同一个公式，
包括那个易错的虚拟份额项（`10 ** _decimalsOffset()`，实为 1e12，不是 1e18 —— 该项目的
`test/price.test.ts` 记录了一个由此产生的 4% 误差）。两处实现必然发散。

现在页面**只读**服务的 `price`（`priceResult.value.series.at(-1)?.price`）。
服务不可用时显示 `—` 并注明 "the index service is unavailable"，
**而不是**临时算一个可能与旁边的索引不一致的数。

### 2.2 两个来源的失败各自独立

`Promise.allSettled`（不是 `Promise.all`）是结构性的，不是风格选择：

- 用 `Promise.all`：任一来源挂掉 → 整页 500。**这正是本项目第一版的症状**。
- 用 `Promise.allSettled` 但"取第一个成功的结果"：会把过期数字标成当前数字。
- 用 `Promise.allSettled` + 逐面板独立渲染（现在）：`Now` 可以失败而 `Then` 仍显示历史，
  反之亦然，且 `Two sources` 面板在两者不齐时**拒绝作答**。

---

## 3. F3 检查记录（G-F3 要求两条证据：静态检查 + code review）

| 证据 | 谁做 | 日期 | 命令 / 范围 | 结论 | 原始输出在哪 |
|---|---|---|---|---|---|
| 静态检查（全仓搜索禁止模式） | DSH agent | 2026-09-16 | `node tools/check-single-source.mjs`（扫描 `src/` 与 `tools/` 全部 `.ts/.tsx/.mts/.js/.jsx/.mjs`，排除 `node_modules` 与 `.next`；允许清单只有 `src/lib/format.ts` 一个文件） | **0 违规，退出码 0** | `docs/evidence/check-single-source-clean.txt` |
| **静态检查器的反证**（证明它该报的会报） | DSH agent | 2026-09-16 | 放入 `src/lib/_violation-probe.ts`（含 5 类违规的构造样本）后跑同一命令 | **7 条违规、退出码 1**，5 条规则全部命中；删除后恢复退出码 0 | `docs/evidence/check-single-source-violation.txt` |
| code review（人读源码，确认无第二事实来源） | DSH agent | 2026-09-16 | 通读 `src/lib/format.ts`、`src/lib/api.ts`、`src/lib/chain.ts`、`src/lib/chartGeometry.ts`、`src/lib/endpoints.ts`、`src/lib/deployment.ts`、`src/app/page.tsx`、`src/components/PriceChart.tsx` | 唯一实现点就是 `format.ts`；`chartGeometry.ts` 内部**只有像素几何**的 `Number`（不碰金额，且它自己文件头写明这一点）；`PriceChart.tsx` 无算术；`page.tsx` 无算术 | 本表 + `证据地图.md` §2 |

**禁止模式清单**（全仓不得出现；出现即"第二事实来源"）

- `10 ** decimals`（任何形式的精度常量）—— 只准出现在 `src/lib/format.ts` ✅ 已由检查器强制
- 组件里的金额算术（`BigInt` 除法、`toFixed`、参与金额的 `Number(...)`）✅ 已由检查器强制
- **手抄的合约地址、chainId、ABI** —— 地址/ABI 只能来自部署产物或运行时配置
  - 地址：`src/lib/deployment.ts` 从 `deployments/local.json` 读（**没有**硬编码地址）
  - ABI：`src/lib/chain.ts` 的 `VAULT_ABI` / `ERC20_ABI` 是用 `parseAbi([...])` 写的**函数签名**，
    不是编译产物里的 JSON。这是刻意选择：函数签名是**接口契约**（人可读、可核对 ERC-4626 标准原文），
    而地址是**部署实例**（必须读）。两者性质不同，所以处理方式不同——不是"ABI 也手抄了所以违规"。
  - `chainId`：不在源码里出现；`src/lib/chain.ts` 的 `ChainConfig` 只带 `vault`/`asset`，
    `chainId` 由 `next.config.ts` 的 RPC 目标与部署记录决定
- 组件内长期持有链上值的字段（不带 TTL、不带失效条件）✅ 不存在：没有客户端状态容器

**这里的 grep 命令已写出并已复跑**（见上表第一行），不是"检查过"。

---

## 4. 六条铁律 → 本文件对应位置

| F3 铁律 | 本文件哪一节负责 | 证据在哪 | 状态 |
|---|---|---|---|
| 1 只有一处能算钱 | §3 禁止模式清单 | `docs/evidence/check-single-source-clean.txt` + 反证文件 | **已建** |
| 2 不缓存链上状态 | §0 规则 1 + §1 表 | `test/api.test.ts` → `sends no-store`；`page.tsx` 的 `force-dynamic` | **已建** |
| 3 读要自己会刷新 | §0 规则 2、3 | **不适用**（无刷新控件、无定时器）；由 `erc4626-vault` 覆盖 | **不适用 + 理由** |
| 4 写必须区分四态 | §1 表里的"交易状态"行 | **不适用**（无写操作）；由 `erc4626-vault` 覆盖 | **不适用 + 理由** |
| 5 失败不得沉默 | `前端规格.md` §3 失败清单 | 3 类有实现 + 浏览器断言；8 类写明不适用 | **部分适用，已建** |
| 6 不许"看起来对" | `浏览器测试计划.md` §3 强制断言风格 | `tools/browser-assert.mjs`（断言渲染后的 DOM 与坐标属性，不是断言源码里的常量） | **已建** |

> **铁律 3、4 标"不适用"而不是"已建"**，是因为它们描述的能力本界面确实没有。
> 把不适用写成"已建"会是本项目纪律禁止的那类记录。

---

## 5. 关卡

- [x] §1 表里每一行的"谁负责刷新"和"刷新触发方式"都有答案（**空白即未通过**）——无空行
- [x] 每个"可缓存 = 是"的行都有 TTL 数值与失效条件（只有"部署记录派生值"与"图表几何"两处标"是"，均已写明条件）
- [x] §3 两条证据齐全，且静态检查命令可复跑（并附**反证**）
- [x] 示例行已全部删除（表内无 `【示例】`）

**G-F2 / G-F3 结论**：**通过（自评）** —— 2026-09-16，验证人：DSH agent（实现者同时是验证者）。
**这不是客户验收**；`支持矩阵与验收.md` §5 记录了每一条的验证方式与这一声明的性质。
