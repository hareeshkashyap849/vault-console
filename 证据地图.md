# 证据地图（路径 B · F2）— vault-console

> **F2 产出物之二。** 关卡 G-F2 要求三件齐备，本文件是第二件。
> 定义见 `../../playbooks/前端正确性设计准则.md` §2.2；依据：`../../工程蓝图.md` §12.2。
>
> **这张表回答一个问题：每条功能的正确性，是谁产生证据的？**
> 判据：如果某个功能的"证据"栏只能写"我看过了"，那它就是**不可验证的**，必须改设计。
>
> **本项目的证据层次**（三层都有真环境证据，这是它区别于模板假设的地方）：
>
> | 层 | 是什么 | 在哪 |
> |---|---|---|
> | 纯函数层 | `format.ts` / `chartGeometry.ts`，无 DOM、无网络 | `test/format.test.ts`、`test/chart-geometry.test.ts` |
> | 契约层 | 服务的真实响应形状（**逐字抓取的字节**） | `test/contract.test.ts` + `test/fixtures/` |
> | 真环境层 | **真浏览器 + 真链 + 真索引服务 + 真 Next 服务端** | `tools/browser-assert.mjs`、`test/contract-live.test.ts` |

---

## 0. 三条硬规则（逐条对照）

1. **每条"不能证明什么"都必须写。这一栏留空 = 本文件未完成。**
   → §2 每一行都填了两栏，**无空行**。
2. **不许由被测代码推导期望值。**
   → 本项目有**三处**期望值来自被测代码之外，写在 §2 的"断言类型"里：
   ① 链上 `totalSupply()`/`totalAssets()` 的 RPC 返回值（不是页面算的）；
   ② 从运行中的服务**逐字抓取**的 fixture 字节（不是照类型手写的）；
   ③ 由两家独立服务（`/api/summary` 与 `/api/price`）给出的数字满足的恒等式。
   反例也记下来了：见 §4 的 `expected-from-code` 一条。
3. **集成/替身层绝不能是"关于真实链的主张"的唯一证据。**
   → 本项目**只有一个替身**（`test/api.test.ts` 里的 `fetch` 替换，见 `替身清单.md`），
   它只支撑"分类逻辑"，**不支撑任何关于真实链的主张**。§3 逐行说明。

---

## 1. 项目功能 → 证据（逐行填实）

| 功能 | 这条功能的正确性由谁产生证据 | 证据的具体位置 | 这条证据能证明什么 | 这条证据不能证明什么 |
|---|---|---|---|---|
| **raw base unit → 十进制字符串**（`formatBaseUnits`） | **纯函数测试**，期望值是手工算出的外部向量 + 与链上实读值比对 | `test/format.test.ts` → `formatBigInt / formatBaseUnits -- RAW BASE UNITS ...` 共 8 条 | 给定 decimals 与 raw 字符串，输出字符串正确；且**大数（19 位）不失精度** | **不能**证明 decimals 是从链上读来的——写死的 18 也可能是错的。decimals 的来源由 `src/lib/chain.ts` 的 `decimals()` 读负责，其正确性由 INV-01 的浏览器断言（与链上 `totalSupply()` 比对）间接覆盖 |
| **十进制字符串 → raw base unit**（`parseAmount`） | **纯函数测试**，期望值为手算值 | `test/format.test.ts` → `parseAmount -- decimal string to base units` 共 8 条 | 换算精确；多于小数位时**抛错而不是截断** | **不能**证明调用方传的 decimals 对。本界面只在 `test/contract.test.ts` 里用它解析蜡烛值，生产路径上**没有**调用点（只读界面不需要反向换算）——这一点是诚实的：`parseAmount` 目前是测试专用工具 |
| **服务已格式化的十进制 → 显示串**（`formatDecimal`） | **纯函数测试**，期望值为手算值 + 等价类 | `test/format.test.ts` → `formatDecimal` 5 条、`displayDecimal` 4 条 | 尾随零归一化；不带小数点的整数**不被改动** | **不能**识别"这个值其实是 raw base unit"——它做不到，原因写在 `格式说明`（INV-04）。这个方向的防线在 `formatBaseUnits` 的拒绝逻辑，不在它这里 |
| **零区间守卫**（平盘序列必须可画） | **纯函数测试**（性质断言）+ **真浏览器坐标属性断言** | `test/chart-geometry.test.ts` → `expands a flat range instead of dividing by zero` 等 4 条<br>`tools/browser-assert.mjs` → `NO NaN coordinates`（172 线 / 169 矩形 / **0 NaN**） | 缩放函数**对全部输入**都产生有限坐标；且浏览器**真的**把这些坐标画出来了 | 纯函数层**不能**证明浏览器画了东西（NaN 在 SVG 里是静默的）。浏览器层**不能**证明别的序列形状也安全（它只看到这一个平盘数据集）。两层缺一不可——这正是两条都保留的原因 |
| **蜡烛几何**（doji 最小高度、方向判定、横向布局） | **纯函数测试** | `test/chart-geometry.test.ts` → `candleGeometry -- the doji floor` 6 条、`horizontalLayout` 4 条 | 零高度实体有可见最小值；方向按 open/close 而非 high/low 判定；1 根与 5000 根都有合法宽度 | **不能**证明像素层面的观感（1.5px 的 doji 是否真的看得见，是视觉判断，见 `不变式台账.md` MAN-03） |
| **两个来源独立失败** | **结构断言 + 真服务**（部分） | `src/app/page.tsx` 的 `Promise.allSettled` + 四个独立分支<br>`test/api.test.ts` → 失败分类 6 条<br>`tools/browser-assert.mjs` → `the four panels rendered`、`no service-failure panel is showing` | 分类逻辑正确；两者都可用时四个面板都在、无失败文案 | **不能**证明"停掉一个服务后另一个面板仍在"——因为断言跑的时候两个服务都是活的。所需的三步停服务复跑见 `浏览器测试计划.md` §5，状态为**未跑**。这是本项目最大的一处证据缺口，**已登记** |
| **`fetch` 失败被正确分类**（不可达 / 拒绝 / URL 非法） | **替身**（替换 `fetch`） | `test/api.test.ts` → `indexApi -- failure classification` 6 条 | 三类失败产生不同**文案**与不同 `kind` 值 | **不能**证明真实 axios/undici/Next 在超时、TLS 错误、限流时抛的**形状**与替身一致。`fetch` 的 TypeError 在 Node 不同版本里措辞会变——本项目只对 `Failed to parse URL` 与 `Invalid URL` 两个子串做过匹配。真形状的证据见 §3 最后一行 |
| **服务响应形状 = 类型声明**（INV-06） | **外部真值**：从运行中的服务逐字抓取的 fixture + **线上重取** | `tools/capture-fixtures.mjs`（生成器）<br>`test/contract.test.ts` → 四个 `has exactly the declared fields` 等 21 条<br>`test/contract-live.test.ts` → 四个 `has the fields … declares` 等 6 条（**线上，6/6 通过**） | 字段名集合**逐一相等**（多一个或少一个都失败）；类型与线上服务一致 | **不能**证明**值**的正确性——它只说"形状对"。字段名的正确性与**语义**的正确性是两件事：`totalSupply` 这个字段叫对了，不代表里面的数字是份额而不是 base unit（那正是 INV-01 的坑，而形状检查完全看不出来） |
| **流量恒等式**（`Deposit − Withdraw + Yield = totalAssets`，INV-05） | **跨组件一致性**：两个独立端点 × 链上值 | `test/contract.test.ts` → `reconciles to the vault's total assets`、`the naive per-kind sum does NOT equal totalAssets`<br>`test/contract-live.test.ts` → `the flow identity holds against the chain` | 索引的事件表与链上状态对得上；且"朴素相加"这个陷阱被单独钉住 | **不能**证明**每一个**事件都被索引到了。`lagBlocks: 0` 只说明索引器追到了它上次看到的链头；如果某个区块的日志被索引器漏读，恒等式**仍然可能成立**（漏掉的是一笔存入和一笔取出的情形下）。覆盖缺口的定性说明来自服务的 `coverage.note`，由页面逐字显示——但那是服务**自己的声明**，不是本项目独立验证的结果 |
| **覆盖缺口被如实呈现** | **真服务**（服务自己发的字段）+ 浏览器文本断言 | `test/contract.test.ts` → `reports the gap honestly: the series starts later than the deployment`（断言 `startsLaterThanDeployment === true` 且 note 含 `NOT a period of zero activity`）<br>`tools/browser-assert.mjs` → 断言页面文本包含 `the price did not move in this window` | 服务确实发了这个字段与这段文字；页面确实把它渲染出来了 | **不能**证明这个缺口是**不可避免**的。服务说"节点不保留那么早的状态"，本项目没有独立复核这一点（例如换一个 archive 节点重试）。已登记为遗留事项 |
| **`totalSupply` 在页面上是份额、不是 raw uint256**（INV-01） | **真链 + 真浏览器**：断言读链的 `totalSupply()`，再断言**渲染后的页面文本** | `tools/browser-assert.mjs` → `totalSupply rendered as SHARES, not as the raw uint256`、`the raw uint256 string does NOT appear as a standalone figure` | 在这个链、这个部署、这次渲染下，页面显示的是份额数，且 raw uint256 字符串**不在页面文本里** | **不能**证明任何**别的** vault 也正确——换一个 18 位小数的资产，raw 值会大到一眼可辨，而本项目的断言用的是硬编码的 vault 地址。这个断言是**针对本次部署**的，不是通用的 |
| **图表 tooltip 携带服务的原始字符串** | **真浏览器**：读 SVG `<title>` 的文本 | `tools/browser-assert.mjs` → `a candle tooltip carries the exact stored strings` | tooltip 里确实是 `open  1.1` 这种服务给的字符串，而不是被格式化过的新串 | **不能**证明 tooltip 在视觉上可读（它只在悬停时出现，且本项目的截图里看不到）。`role`/`aria` 层面也没有把 tooltip 暴露给辅助技术 |
| **`no-store` 确实被送出**（不缓存） | **替身**：捕获 `fetch` 收到的 `init` 对象 | `test/api.test.ts` → `sends no-store, because a cached console shows stale figures as current` | 客户端确实把 `cache: 'no-store'` 与 `accept: application/json` 传给了 `fetch` | **不能**证明 Next.js / 中间层**尊重**这个头。真正的证明是"改一次链上状态，刷新页面，数字变了"——那个证据见 `浏览器测试计划.md` §5 的"数据新鲜度"一行 |
| **`dynamic = 'force-dynamic'` 生效** | **无自动化断言** | `src/app/page.tsx` 第 28–29 行 | — | **不能**证明任何东西。这一条**目前只有源码上的声明**。可用的检验是：两次请求返回不同内容（`lagBlocks` 或时间戳变化），本项目**没有**写成断言。已登记为遗留事项 §4 |

---

## 2. 必须有真环境证据的主张（不许只由替身支撑）

下表左列的主张，**若只有替身层证据，则视为未验证**。

| 主张 | 只有替身时缺什么 | 真证据在哪（真链 / 真钱包 / 真浏览器） | 状态 |
|---|---|---|---|
| 界面读数与链上数值相等 | 假链是"我们以为的链" | `tools/browser-assert.mjs`：断言读**真 anvil 的 `eth_call` `totalSupply()`**，再与**真浏览器渲染出的页面文本**比对。链 `0x9fE4…a6e0`，实测 `859021905704231281673` ↔ 页面 `859.021905704231281673` | **已验证** 2026-09-16 |
| 真实渲染下元素可见/不可见 | jsdom 类环境没有布局与层叠 | `tools/browser-assert.mjs` 通过 kimi-webbridge 驱动**用户真实的 Chrome**（CDP），读的是**渲染后 DOM** 的 `getAttribute`，不是源码常量 | **已验证** 2026-09-16 |
| 真实 RPC/API 的失败形状（字段名、超时、状态码） | 手写响应是我们想象的形状 | `tools/capture-fixtures.mjs` 从**运行中的服务**逐字抓取 4 个端点的响应（`sha256` 前缀记录在 `docs/evidence/`）；`test/contract-live.test.ts` 对**线上服务**重取并断言，**6/6 通过** | **已验证** 2026-09-16 |
| 真实服务端渲染（不是客户端补出来的） | 客户端渲染能掩盖服务端错误 | `tools/browser-assert.mjs` 先 `fetch(PAGE)` 断言 **HTTP 200** 与 HTML 不含错误页，**再**用浏览器打开同一 URL。第一版的 HTTP 500 就是这样被定位的 | **已验证** 2026-09-16 |
| 价格序列确实是平的（INV-02 的前提） | 手造数据不能代表真实库 | `test/contract.test.ts` → `every candle in the captured window is flat at 1.1`：断言 fixture 里 169 根蜡烛**只有一个不同的价格字符串** | **已验证** 2026-09-16 |
| **停掉一个服务后另一个面板仍在**（INV-03 的核心） | 没有替身能模拟"真进程死掉" | **无。** 三步停服务复跑写在 `浏览器测试计划.md` §5，**尚未执行** | **未验证** |
| 真实钱包的拒签行为是 `4001` | 错误码是我们自己注入的 | **本项目不适用**（只读，不连钱包）。覆盖项目 `erc4626-vault` 已用真 MetaMask 13.48 实测：拒签报中性消息、不产生链上花费 | **不适用 + 已由姊妹项目覆盖** |
| 交易真的上链并被确认 | 假链不真的打包 | **本项目不适用**（只读，不发交易）。覆盖项目 `erc4626-vault` 有真实交易哈希与区块号 | **不适用 + 已由姊妹项目覆盖** |

> **真实教训（本工作区 P4）**：真实 API 用小写下划线字段名而类型标注是大驼峰，
> 类型断言把编译器的投诉压掉了，最后表现为运行时报"不能把 undefined 转成 BigInt"。
> **手写的响应样本会掩盖这种错误；录制的真实响应不会。**
>
> 本项目**独立复现了同一个教训的另一面**：`SummaryResponse` 声明了 `counts`/`totals`，
> 服务发的是 `kinds`/`totalEvents`。因为是**手写**的类型，它和**没人调用它**这两件事
> 加起来，让这个错误连一次运行时错误都没产生过。修法就是上面那条：
> fixture 由 `capture-fixtures.mjs` 从服务抓，期望形状由 `contract.test.ts` 断言，
> 而线上由 `contract-live.test.ts` 重取。

---

## 3. 现在还没有证据的功能（诚实列出）

| 功能 | 为什么现在没有证据 | 计划（谁 / 何时） | 风险接受人 |
|---|---|---|---|
| **两个来源独立失败**（停一个服务，另一个面板仍在） | `browser-assert.mjs` 运行时两个服务都是活的，所以它断言的是"都活着时四个面板都在"。要证明独立性必须**真的停掉一个进程**，而它会打断同一次验证会话里的其它断言 | 三步复跑命令已写进 `浏览器测试计划.md` §5（状态：未跑）。DSH agent / 下一轮 | DSH agent（作品集自评，无外部客户） |
| **价格变动过的数据集**（非平盘的图表） | 本机库的份额价格全程 `1.1`（每次 yield report 同比例抬高 `totalAssets` 且不铸币），所以真实数据**只有平盘一种**。平盘之外的路径只有合成蜡烛的单元测试覆盖 | 造一个价格确实变动过的库（`reportYield` 改成不等比例，或直接指向另一个服务实例），再跑一次 `browser-assert.mjs`。DSH agent / 未排期 | DSH agent |
| **`dynamic = 'force-dynamic'` 生效** | 没有写成断言 | 加一条：连续两次 `fetch(PAGE)`，断言响应内容不同（时间戳或 `lagBlocks` 变化）。DSH agent / 未排期 | DSH agent |
| **可访问性基线**（键盘可达、对比度、屏幕阅读器） | 没有跑过任何审计工具，也没有跑过屏幕阅读器。语义地标与 `role="img"` + `aria-label` 是**写进去**的，但"写进去"不是证据 | 需要装审计工具或人工过一遍键盘路径。`支持矩阵与验收.md` §4 记为 `未测`。DSH agent / 未排期 | DSH agent |
| **构建可复现性**（两次构建产物哈希相同） | `next build` 的产物含时间戳与随机 chunk 名，两次构建的哈希**本来就不相同**；要证明可复现需要比对的是**依赖锁**与**源码哈希**，而不是产物哈希 | 改为记录 `package-lock.json` 的哈希 + 源码树哈希。DSH agent / 未排期 | DSH agent |
| **覆盖缺口是否不可避免** | 服务说"节点不保留那么早的状态"，本项目没有独立复核 | 换 archive 节点重跑索引器，看能否补上区块 8–167。DSH agent / 未排期 | DSH agent |

> 本节**不为空**，六条都写了"为什么没有"与计划。
> 其中"两个来源独立失败"是**唯一一条与核心设计主张直接相关**的缺口，
> 因此它在 `不变式台账.md` INV-03 与 `支持矩阵与验收.md` 里都被单独点名。

---

## 4. 一处已修正的错误期望值（留痕）

按准则 §结构 3，"由被测代码推导出的期望值"是无效证据。本项目的契约测试**在写作过程中**
出现过一次这个问题，值得记录，因为它说明写契约测试本身也会走偏：

| 位置 | 当时的写法 | 问题 | 改法 |
|---|---|---|---|
| `test/contract.test.ts` → 蜡烛 OHLC 有序性 | `const [open, high, low, close] = [c.open, ...].map((v) => BigInt(v as string))` | 不是"由被测代码推导"，而是**假设错了数据形状**：蜡烛的 OHLC 是服务**已格式化**的十进制字符串 `"1.1"`，`BigInt('1.1')` 直接抛 `SyntaxError` | 改用 `parseAmount(c.open, 6)`——本项目自己的精确解析函数，同时也让这条断言顺带覆盖了"服务发的确实是 6 位小数的十进制串" |

**这条记录的意义**：期望值的**来源**正确（来自服务的真实响应），但**解读**错了。
契约测试第一次运行时把自己的作者抓了出来，这是它按预期工作而不是失败。

---

## 5. G-F2 关卡（本文件部分）

- [x] 每个"功能"都有"谁能证明它"和"它不能证明什么"两栏内容（14 行，无空栏）
- [x] §2 的八行都有真证据位置，或明确写着"未验证"并说明计划
- [x] 示例行已全部删除（表内无 `【示例】`）
- [x] 台账里的每条不变式，都能在本文件里找到它依赖的证据来源（INV-01…INV-08 逐条对应）

**G-F2 结论**：**通过（自评）** —— 2026-09-16，DSH agent。
14 行功能证据齐备，7 项"不能证明什么"逐条写明；§3 诚实列出 6 项**尚无证据**的功能，
其中 1 项与核心设计主张直接相关。
**不是客户验收**，理由见 `支持矩阵与验收.md` §5。

> 配套：`不变式台账.md`（要证明什么）、`替身清单.md`（替身的严格程度是否够格当证据）。
