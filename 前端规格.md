# 前端规格（路径 B · F1）— vault-console

> **F1 产出物。关卡 G-F1：用户可见行为与失败清单书面化，且客户确认。**
> 依据：`../../工程蓝图.md` §12.2；方法论：`../../playbooks/前端正确性设计准则.md`。
> **本文件只写主张，不含证据。** 每条主张的证据位置写在 `证据地图.md`。
>
> **本项目的特殊之处（先说清楚，因为它改变了 §3 的形状）**：
> 这是一个**只读控制台**。它不连接钱包、不签名、不发送交易。
> 因此模板假设的 11 类失败里，**只有 3 类在本界面存在**。
> 剩下的 8 类逐行写 **`不适用 + 理由`**——按骨架纪律，留空 = 未完成，
> 而"不适用"必须给出理由，不能只写两个字。
> 这 8 类的实现与验证在姊妹项目 `erc4626-vault`（钱包 dApp，路径 B 的写侧）里，
> 本文件在对应行写明"由哪个项目覆盖"，而不是假装它们不存在。

---

## 0. 元信息

| 项 | 值 |
|---|---|
| 项目 | vault-console（只读 ERC-4626 控制台，路径 B） |
| 本文件版本 / 日期 | v1 / 2026-09-16 |
| 填写人 | DSH agent（执行区） |
| 客户确认人 / 日期 | **【待客户】** 本工作区为自建作品集，无外部客户；见 §5 关于"确认人"的处理 |
| 目标链 chainId | **31337**（Anvil 本地链）。对照主源：`../erc4626-vault/deployments/local.json` 的 `chainId` 字段，与 `eth_chainId` 实测 `0x7a69` = 31337 一致。**不是**凭记忆填的；本机 anvil 默认端口 8545 |
| 界面读数的权威来源 | **两者兼有，且不合并**：`Now` 面板 = **链上直读**（本次请求）；`Then` / `Index health` 面板 = **索引 API**；`Price per share` = **索引 API**；`Two sources` 面板 = 两者并列展示。**冲突时不信任何一个**——该面板在两者只有其一时拒绝作答（见 UI-04） |
| 钱包接入方式 | **不适用 + 理由**：本界面为只读，不注入 provider、不检测 `window.ethereum`、不请求账户。这样做的理由是"读数不需要授权"：一个只需要读的界面要求连接钱包，会在用户还没看到任何数字之前就要走一次授权弹窗 |

---

## 1. 界面清单与可验收陈述

**陈述的写法**：给定 <前置条件>，当 <用户动作或系统事件> 时，界面上出现 / 不出现 <可观察结果>。
以下每条都在 `tools/browser-assert.mjs` 里有对应断言（见 `证据地图.md`），
或明确标注为"未验证"。

| 编号 | 界面 / 组件 | 用户在这里做什么 | 可验收陈述（可观察结果） | 客户确认（人 / 日期） |
|---|---|---|---|---|
| UI-01 | 整个页面 | 打开 `/` | 给定链与索引服务都在运行，当页面加载完成时，页面上出现 **4 个 `<section>` 面板**（标题依次为 `Now` / `Then` / `Two sources, checked against each other` / `Index health`），且**不出现**任何 `could not be read` / `not reachable` / `refused` 文案 | 【待客户】 |
| UI-02 | `Now` 面板 · Total shares | 无动作（被动显示） | 给定链上 `totalSupply()` 返回 `859021905704231281673`（raw base unit），当页面渲染时，该格显示的是**带小数点的份额数**（形如 `859.021905704231281673`），**且该 raw uint256 字符串全文不出现在页面上任何位置** | 【待客户】 |
| UI-03 | `Now` 面板 · Total assets | 无动作 | 给定链上 `totalAssets()` 返回 `944924100`（raw base unit，资产 6 位小数），当页面渲染时，该格显示 `944.9241 USDC`，而**不是** `944,924,100` | 【待客户】 |
| UI-04 | `Two sources` 面板 | 无动作 | 给定链**或**索引服务任一不可用，当页面渲染时，该面板显示 **"A comparison needs both sources. One of them is unavailable, so this panel does not guess."**，且**不显示**任何由单一来源推出的对比结论 | 【待客户】 |
| UI-05 | `Now` 面板 · 链不可达 | 无动作 | 给定 RPC 不可达，当页面渲染时，`Now` 面板显示标题 **"The chain could not be read."** 加具体错误句，**而 `Then` 面板仍然正常显示历史**（两个来源独立失败） | 【待客户】 |
| UI-06 | `Then` 面板 · 索引不可达 | 无动作 | 给定索引 API 不可达，当页面渲染时，`Then` 面板显示 **"The index service could not be read."**，**而 `Now` 面板仍然显示当前链上读数** | 【待客户】 |
| UI-07 | `Then` 面板 · K 线图 | 鼠标悬停某根蜡烛 | 给定价格序列全程为 `1.1`（区间为零），当图表渲染时，画面中出现 **一条位于面板中部的水平线**（不是空白面板），且 **SVG 中不存在任何含 `NaN` 的坐标属性** | 【待客户】 |
| UI-08 | `Then` 面板 · 图注 | 无动作 | 给定价格全程未变，当图表渲染时，图注中出现文案 **"the price did not move in this window"** ——平盘必须被**说出来**，不能留给读者从一条平线自行推断 | 【待客户】 |
| UI-09 | `Then` 面板 · 覆盖缺口说明 | 无动作 | 给定索引服务的 `coverage.startsLaterThanDeployment === true`，当页面渲染时，出现服务自己那句 **"This is a gap in the data, NOT a period of zero activity -- do not draw or read it as one."**（**逐字照抄，不改写**） | 【待客户】 |
| UI-10 | 蜡烛 tooltip | 悬停 | 给定某根蜡烛的 OHLC 均为 `1.1`，当悬停时，tooltip 中出现**服务返回的原始字符串**（`open  1.1` / `high  1.1` / `low   1.1` / `close 1.1`）与区块区间（形如 `25 blocks (1249-1273)`） | 【待客户】 |
| UI-11 | 页脚 | 无动作 | 页面上出现文案 **"Read-only. Nothing on this page writes to the chain, and no wallet is required or requested."** | 【待客户】 |
| UI-12 | 页头 · record | 无动作 | 出现部署记录的来源路径尾部（`erc4626-vault/deployments/local.json`），使读者知道这组地址是从哪个文件读的 | 【待客户】 |

### 关于 UI-02 / UI-03 的由来（两处真实缺陷，不是假想）

这两条陈述来自本仓库**自己**的两个 bug，都被浏览器断言抓到：

1. **UI-02 的 raw base unit**：第一版把 `totalAssets` 与 `totalSupply` 走同一个格式化函数，
   而服务对这两者返回的是 **raw uint256 base unit**（`../erc4626-vault-dapp/src/api/price.ts`
   原文：*"RAW uint256 values in BASE UNITS, exactly as"*）。于是页面把份额印成
   `859,021,905,704,231,281,673` —— **正是"绝不显示"清单第 2 条**。
   它在 `totalAssets` 上看起来完全正常，纯粹因为资产是 6 位小数、`944924100` 像一个普通数字。
2. **UI-03 的同源缺陷**：同一个函数把 `944924100` 原样印成 `944,924,100`，
   即把 944.9241 USDC 显示成九亿四千四百万。**它看起来比真值更"像钱"**，所以更危险。

修法是把"raw base unit"与"服务已格式化"分成两个函数
（`displayBaseUnits` / `displayDecimal`），并让 `formatBaseUnits` **拒绝**带小数点的输入。
`前端规格.md` §2.2 与 `状态所有权.md` §3 记录了这个唯一实现点。

---

## 2. 金额与小数位的唯一来源

### 2.1 精度参数（每个都必须从链上读，不得写死）

| 参数 | 值 / 从哪读 | 主源核对日期 | 谁负责填 |
|---|---|---|---|
| 资产合约地址 | **不手抄**。`src/lib/deployment.ts` 读 `../erc4626-vault/deployments/local.json` 的 `asset` 字段（运行时读文件） | 2026-09-16 | `deployment.ts` |
| 金库合约地址 | 同上，`vault` 字段。两份记录（链上部署脚本写的、索引器读的）是**同一个文件** | 2026-09-16 | `deployment.ts` |
| `assetDecimals` | **运行时读 ERC-20 `decimals()`**（`src/lib/chain.ts` 的 `readDeployment`，走 `ERC20_ABI`）。UI 里的 `live?.assetDecimals ?? 6` 只在链不可达时兜底，且那种情况下 `Now` 面板整块显示错误而不是显示数字 | 2026-09-16 | `chain.ts` |
| 份额 `decimals` | **运行时读金库 `decimals()`**（同一处批量调用里的 `VAULT_ABI.decimals`） | 2026-09-16 | `chain.ts` |
| 虚拟份额 / offset 项 | **本界面不适用 + 理由**：`_decimalsOffset()` 只在把 base unit 换算成"份额价格"时才需要，而**本界面不做这个换算**——份额价格由索引服务算好（`../erc4626-vault-dapp/src/api/price.ts` 的 `sharePrice()`，它在那里有独立测试并已核对过 offset 用法）。控制台只读结果。这是刻意的：同一道易错公式在两处实现必然发散 | 2026-09-16 | —（由服务负责） |
| 各方向的舍入规则 | **本界面不适用 + 理由**：只读控制台不做 `convertToShares` / `convertToAssets`，也没有任何写操作，因此不存在"向上/向下取整"的选择。金额只做**十进制 ↔ base unit 的字符串换算**，那是精确整数运算，不涉及舍入。`sharePercent()` 是唯一的除法，且**刻意截断**而非四舍五入（截断使显示值永远不会大于真值） | 2026-09-16 | — |

### 2.2 唯一实现点（G-F3 的"只有一处能算钱"）

| 项 | 值 |
|---|---|
| 唯一实现点（文件） | **`src/lib/format.ts`** |
| 函数：base unit → 十进制字符串 | `formatBaseUnits(baseUnits: string \| bigint, decimals: number): string`（内部走 `formatBigInt`） |
| 函数：十进制字符串 → base unit | `parseAmount(value: string, decimals: number): bigint` |
| 函数：服务已格式化的十进制 → 显示串 | `formatDecimal(value: string): string` |
| 函数：份额 ↔ 资产 换算 | **不适用 + 理由**：见 §2.1 最后两行。控制台不做换算；它读服务算好的 `price` |
| 是否纯函数（无 DOM、无网络、无全局状态） | **是**。`format.ts` 只 import `node:assert` 之外的零依赖；不读 `process.env`、不碰 `fetch`、无模块级可变状态。`endpoints.ts` 是唯一读 env 的地方，且它是 URL 组装，不碰金额 |
| 它的测试 | `test/format.test.ts`（46 个测试）与 `test/contract.test.ts`（21 个测试） |
| 输入约定 | 金额一律 `bigint` 或**十进制字符串**；`formatBaseUnits` 收 **raw 整数字符串**。字符串绝不含科学计数法，绝不带千分位。`Number` 只允许用于区块号/时间戳/计数/小数位个数 |
| 输出约定 | 小数位 = 该资产的 `decimals`，**去掉尾随零**（使 `1.1` 可断言，而不是 `1.100000000000000000`）；默认**千分位分组**，可用 `{ group: false }` 关掉；**不存在截断或四舍五入**（换算精确）；`sharePercent` 是唯一例外，两位小数且**截断** |

**规则（违反即 G-F3 不通过）**

- 全仓**只允许** `src/lib/format.ts` 出现：`10 ** decimals`、`BigInt` 除法、`toFixed`、
  以及任何参与金额计算的 `Number(...)`。
- 其它地方只准**调用**它。UI 组件里出现算术 = 第二事实来源。
- 送进钱包的每个数额必须经过唯一实现点：**不适用 + 理由**——本界面不给钱包送任何数额（只读）。
  该要求在姊妹项目 `erc4626-vault` 的 `web/app/*` 里适用并已实现（Max 按钮填的是十进制字符串）。
- 静态检查命令（可复跑，**不是"检查过"**）：

  ```powershell
  node tools/check-single-source.mjs
  # 期望输出：
  #   Scanned 9 source files (excluding 1 allowed and all tests/tools).
  #   Amount module: src/lib/format.ts
  #
  #   No second source of truth found.
  # 退出码 0
  ```

  **该检查器本身经过反证**：往 `src/lib/` 放一个含 5 类违规的文件后，它报出 **7 条违规、退出码 1**；
  删除该文件后恢复退出码 0。两次运行的原始输出记在 `docs/evidence/`。见 `证据地图.md` §3。

---

## 3. 失败清单（11 类，逐行必须填）

> **本项目只读，所以 11 类里 8 类不适用。** 本节对每一行给出**明确的适用性判定**，
> 不适用的写出理由与"由哪个项目覆盖"。**留空的格子 = 未完成。**
>
> 只有 3 类在本界面存在：**RPC 不可达** / **API 不可达** / **数据过期**。
> 这三类都有实现与浏览器断言。

| 失败类 | 界面必须显示什么（文案原文） | 是否在发送前拦截 | 复跑方式 |
|---|---|---|---|
| 钱包未安装 | **不适用 + 理由**：本界面不请求钱包、不检测 provider、不发交易，因此"钱包未安装"不改变任何可见行为（UI-11 明确写着 *"no wallet is required or requested"*）。**覆盖项目**：`erc4626-vault`（钱包 dApp） | 不适用 | 不适用 |
| 用户拒签（EIP-1193 `4001`） | **不适用 + 理由**：本界面不发起签名请求，因此不存在 `4001`。**覆盖项目**：`erc4626-vault`（已实测：拒签报 `message neutral`，主观为中性而非错误，且不产生任何链上花费） | 不适用 | 不适用 |
| 链不符 | **不适用 + 理由**：不连钱包就没有"钱包所在链"。控制台读的链由 `VAULT_RPC` 与部署记录的 `chainId` 决定，记录里的 `chainId` 会显示在页头 | 不适用 | 不适用 |
| 授权不足 | **不适用 + 理由**：只读，无 ERC-20 allowance 概念。**覆盖项目**：`erc4626-vault` | 不适用 | 不适用 |
| 余额不足 | **不适用 + 理由**：只读，不花费。**覆盖项目**：`erc4626-vault`（已实测：超额输入不发送任何交易） | 不适用 | 不适用 |
| gas 不足 | **不适用 + 理由**：只读，不发交易。**覆盖项目**：`erc4626-vault` | 不适用 | 不适用 |
| 交易 revert | **不适用 + 理由**：只读，不发交易。**覆盖项目**：`erc4626-vault` | 不适用 | 不适用 |
| 交易被替换（replaced） | **不适用 + 理由**：只读，不发交易，无 pending 状态。**覆盖项目**：`erc4626-vault` | 不适用 | 不适用 |
| **RPC 不可达** | **`"The chain could not be read."`** 加该次失败的具体消息。`Now` 面板整块替换为这个错误框，**不显示任何过期数字**；`Then` 面板不受影响，继续显示历史 | 否（为什么不可能在发送前判断：本界面不发送任何东西，"发送前"不存在。**读**的失败只能被观察到） | 停掉 anvil，刷新 `http://localhost:3000`。期望：`Now` 面板出现 `"The chain could not be read."`，且页面仍然 200、`Then` 面板仍显示蜡烛图 |
| **API 不可达** | **`"The index service is not reachable at <实际 URL>. It runs as a separate process; start it with \`node --experimental-strip-types src/api/cli.ts\` in the erc4626-vault-dapp repository."`** ——句子**包含实际请求的 URL**，因为"不可达"这个词单独出现时无法区分"服务没起"和"起错端口" | 否（同上：读的失败只能被观察） | 停掉索引 API（`Ctrl-C` 该进程），刷新页面。期望：`Then` 面板出现上述句子**且含 URL**，`Now` 面板仍显示真实链上读数 |
| **数据过期** | 索引读数**必须标明它是哪个区块的**：`Indexed to block <N>`、`Lag <N> blocks`、`indexer last ran <duration> ago`、以及服务自己那句 `lagBlocks is measured against the chain head recorded at the last indexer run, not against the chain now.`（逐字显示）。另加覆盖缺口说明（UI-09） | 否（本界面不发送） | 让索引器停一段时间（不重跑 catch-up），刷新页面。期望：`Lag` 与 "indexer last ran … ago" 数值随时间增长，且该 note 仍在页面上 |

**填写时必须遵守的三条（来自 F3 铁律 4 与 5）——逐条对照**

1. **`4001` 拒签是中性事件** → **本界面不适用**（不签名）。该规则在 `erc4626-vault` 适用且已实测。
2. **链不符 / 授权不足 / 余额不足必须"发送前拦截"** → **三者本界面均不适用**（无发送、无钱包）。
   本界面**唯一**该被"拦截"的类比是**格式错误的数据**：`formatBaseUnits` 对带小数点的输入
   **抛错**而不是猜（见 `状态所有权.md` §3 的禁止模式）。
3. **`revert` 与 `replaced` 不得与 `success` 共用文案** → **本界面不适用**（无四态，无写操作）。
   本界面里"可区分"的对应要求是**两个来源的错误文案必须不同**：`"The chain could not be read."`
   与 `"The index service is not reachable at …"` 是两句不同的话，且指向不同的修法——这一条**是本界面实测的**（UI-05 / UI-06）。

> 这张表与 `浏览器测试计划.md` §5 的失败场景矩阵**同一套 11 类，编号必须对得上**。
> `浏览器测试计划.md` §5 逐行复述了同样的适用性判定。

---

## 4. 本界面绝不显示什么

| # | 绝不显示 | 为什么 | 违反时由哪一层断言抓到 |
|---|---|---|---|
| 1 | 未确认的余额（把 pending 当已到账） | 用户会按错误余额做决策。**本界面增强为**：不显示任何**未标明来源与区块**的读数 | 浏览器层（每个面板都带来源标签，`browser-assert.mjs` 断言 4 个面板存在且无失败文案） |
| 2 | **原始最小单位整数（`859021905704231281673` 这类）** | **本仓库真的犯过这个错**：份额被印成 raw uint256（见 §1 的说明）。它看起来像一个合法的分组数字，所以骗过了一次阅读 | 纯函数层（`test/format.test.ts`）**+ 浏览器层**（`browser-assert.mjs` 断言 raw uint256 字符串不出现在渲染文本里） |
| 3 | 把 `pending` / `replaced` / `reverted` 显示成"成功" | **不适用 + 理由**：无写操作、无交易状态。**覆盖项目**：`erc4626-vault` | 不适用 |
| 4 | 超过失效时间、又不带"可能过期"标记的旧读数 | 旧值会被当成当前值。**本界面做法**：链上读数每个请求重读（`dynamic = 'force-dynamic'` + `cache: 'no-store'`）；索引读数**始终带区块号**与滞后时长 | 纯函数层（`test/api.test.ts` 断言 `cache: 'no-store'` 确被送出）+ 浏览器层（断言 `Indexed to block` 可见） |
| 5 | **把平盘画成空白图表** | 价格全程 `1.1` 时 `max === min`，`(v-min)/(max-min)` = `0/0` → 所有 y 坐标 NaN → **SVG 静默不画任何东西**。读者会理解为"没有数据"，而实际有 169 根蜡烛 | 纯函数层（`test/chart-geometry.test.ts` 断言 NaN 不存在、span > 0）**+ 浏览器层**（`browser-assert.mjs` 断言 172 条线/169 个矩形中 **0 个** NaN 坐标属性） |
| 6 | **把"我们读不到"画成"那段时间什么都没发生"** | 覆盖缺口（数据从区块 168 开始，但服务从区块 8 就有记录）如果不说明，图表会读作"那段时间金库没有活动"。真相是**我们读不到那段区块**，不是没有活动 | 服务把它写在 `coverage.note` 里；页面**逐字照抄**（UI-09）。断言：`browser-assert.mjs` 检查该句在页面上，`test/contract.test.ts` 断言服务确实发了这个字段与这句原文 |

> 第 1–4 条是**默认条目，不要删**。第 3 条在本项目标为不适用并给出覆盖项目（未删行）。
> 第 5、6 条是本界面独有的追加项，来自本项目自己的两个真实缺陷。

---

## 5. G-F1 关卡

- [x] 每个界面都有可验收陈述，且写成"给定…当…则…"的可观察结果（UI-01…UI-12）
- [x] 金额与小数位的唯一来源已指定到**具体文件 + 具体函数名**（`src/lib/format.ts` §2.2）
- [x] 11 类失败逐行填实，无空行、无"待定"（3 类实现，8 类写明不适用 + 理由 + 覆盖项目）
- [x] "本界面绝不显示什么"的默认条目仍在（第 1–4 条保留，第 3 条标不适用）
- [ ] **客户书面确认**（逐条签字表在 `支持矩阵与验收.md` §5）

**G-F1 结论**：**【待客户确认】** —— 技术条目全部填实，但本工作区是作品集而非客户项目，
没有外部客户签字。`支持矩阵与验收.md` §5 把每一条陈述的"谁验证过 / 用什么验证的"列成表，
由**作者本人**以"实现者 + 验证者"的双重身份签署，并在文件中明确标注这一点是作品集的自评、
**不是客户验收**。把自评写成客户验收会是本工作区纪律禁止的那类记录。

> 下一步：F2 —— `不变式台账.md`、`证据地图.md`、`替身清单.md`、`状态所有权.md`。
