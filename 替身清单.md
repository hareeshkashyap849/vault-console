# 替身清单（路径 B · F2）— vault-console

> **F2 产出物之三。** 关卡 G-F2 要求三件齐备，本文件是第三件。
> 定义见 `../../playbooks/前端正确性设计准则.md` §2.3；依据：`../../工程蓝图.md` §12.2。
>
> **替身（test double）= 任何不是真实对象的东西**：假链、假 RPC、DOM 桩、
> 注入的假钱包、手写的 API 响应样本、假计时器。
> **它们和真实对象总会发散**——真实对象在演进而替身不会。
> 这张表的作用是：把"它简化掉了什么"写在明处，而不是让它悄悄吃掉一个 bug。
>
> **本项目的替身极少，这是设计的结果而不是省略的结果。**
> 只读控制台不需要钱包装置、不需要假链、不需要 DOM 桩（因为**没有** jsdom 测试层——
> 界面层全部由真浏览器断言覆盖）。全部替身只有**一个**：`test/api.test.ts` 里对
> `fetch` 的替换。另有**一处"不是替身但要说清楚"的东西**（fixture），见 §2。

---

## 0. 两条硬规则（逐条对照）

1. **替身必须比真实对象更严格，或至少同等严格，绝不更宽容。**
   → 本项目的替身逐项对比见 §2。**结论：在它被用来验证的范围里同等严格，
   在它无法覆盖的范围里明确标注为"做不到"**（§3），而不是假装覆盖。
2. **替身若由被测代码推导期望值，则该测试无效。**
   → 本项目替身的期望值是**手写的**（`'http://127.0.0.1:8787/api/status'` 这类字面量），
   不是由客户端算出来的。这一点很关键：如果期望值来自 `apiUrl()` 本身，
   那么客户端把 URL 拼错时，断言会跟着一起错。

---

## 1. 本项目替身（逐行填实）

| 替身 | 它替代的真实对象 | 它简化了什么 | 为什么这个简化不掩盖被验证的行为 | 它是从哪里加载的 | 谁负责在真实对象变化时更新它 | 判定 |
|---|---|---|---|---|---|---|
| **`globalThis.fetch` 的替换**（`test/api.test.ts` 的 `stubFetch`） | Node 的真实 `fetch`（undici）+ 真实网络栈 + 真实索引服务 | **① DNS / TCP / TLS / 代理**——没有真实连接；**② 服务端的参数校验**——替身按指令回答，不认识 `bucket` 与 `limit`；**③ 真实超时与流控行为**；**④ `Response` 的流式读取**——用的是 Node 内置 `Response`，但内容由我构造 | **不掩盖本项目真正要验的两件事**：① **URL 是不是绝对地址** —— 这恰恰是原缺陷（服务端 `fetch('/api/status')` 抛 `TypeError`），而替身**记录并断言了收到的 URL 字符串**，比真服务更直接：真服务收到错误 URL 只会连接失败，看不清发的是什么；② **失败如何被分类** —— 替身可以精确构造 `TypeError('Failed to parse URL …')`、`TypeError('fetch failed')`、502 非 JSON、200 非 JSON 四种形状，**真服务一种都构造不出来**。这两件事在替身上比在真服务上验得更严 | **手写副本**（`test/api.test.ts` 内联） | 触发条件：**`src/lib/api.ts` 的失败分类逻辑变化**时，或 **Node 改 `fetch` 的 TypeError 措辞**时。责任人：DSH agent | **合格（限此用途）**。另见 §3 的对照测试与残余缺口 |
| **fixture 文件**（`test/fixtures/*.json`）——**严格说不是替身** | 索引服务的**实时**响应 | 时间性：它是**某一个时刻**的响应快照，服务后来会变 | **不掩盖**：它不是手写的，是 `tools/capture-fixtures.mjs` **逐字抓取**的服务输出（只重新缩进以方便 diff）。因此它不会像手写样本那样"编码作者的误解两次"。而"会不会过期"这一风险由 `test/contract-live.test.ts` 直接对**线上服务**重取来覆盖 | **真实实现的产物**（服务的真实字节） | 触发条件：**服务响应形状变化**时，跑 `node tools/capture-fixtures.mjs` 重新抓取。责任人：DSH agent | **合格**——按模板的加载来源优先级，它属于第 1 档"录制的真实响应原文"，不是第 2、3 档 |
| **无假链 / 无假 RPC / 无假钱包 / 无假计时器 / 无 DOM 桩** | — | — | — | — | — | **不适用**：本项目是只读的，没有写路径需要假链；界面层不由 jsdom 验证（**没有 jsdom 依赖**），而是由真浏览器断言覆盖；没有定时器需要假计时器 |

**为什么没有假链**：控制台唯一的链上交互是 `eth_call`（六次 `readContract` 批量发出）。
要验证它，**真实 anvil 比假链更容易也更可信**——anvil 就在本机 8545 上跑着，
而 `browser-assert.mjs` 直接对它发真实 `eth_call`。**凡能跑真实依赖的层，绝不放替身。**

---

## 2. 替身与真值的对照（防止替身悄悄发散）

> 每条替身至少要有一个**对照测试**：同一输入分别喂给替身与真实对象，断言结论一致。

| 替身 | 对照测试在哪（文件:测试名） | 或：为什么做不到 + 人工核对步骤 | 复核人 | 状态 |
|---|---|---|---|---|
| `fetch` 替换 | **`test/api-against-real-service.test.ts`**（9 个测试，对**运行中的真实服务**跑**同一个客户端类**）。逐项对照：<br>· 替身说 `/api/status` 收到请求 → 真服务答 200 且字段名相符<br>· 替身说 `/api/price?limit=1` → 真服务答 `series.length === 1`、`limit === 1`<br>· 替身说 `/api/candles?bucket=60&limit=5000` → 真服务**接受**这个查询串（这是替身**完全无法**验证的部分）<br>· 替身说非法输入走 `refused` 分支 → 真服务对非数字 `bucket` 答 4xx，客户端分类为 `refused` 且 `retryable === false` | — | DSH agent | **已建**（9/9 通过，`docs/evidence/api-against-real-service.txt`） |
| `fetch` 替换 —— **`cache: 'no-store'` 那一项** | **做不到**：`no-store` 是客户端发给 `fetch` 的**参数**，不是服务端能观察到的响应特征；而真服务无法区分"客户端要求不缓存"与"客户端碰巧拿到一份没被缓存的内容"。<br>**人工核对步骤**：① 读 `src/lib/api.ts` 的 `LIVE` 常量与每个 `indexApi.*` 调用，确认都传了它；② 在真实浏览器里打开页面两次，中间改变链上状态（在 anvil 上发一笔转账），确认第二次刷新数字变了——**这一步尚未执行**，见 `浏览器测试计划.md` §5 的"数据新鲜度"一行 | DSH agent | **部分未验证** |
| `fetch` 替换 —— **transport 失败（超时 / TLS / DNS）** | **做不到，且已登记为残余缺口**：要构造真实超时需要一个会挂起的服务端；要构造 TLS 错误需要一个坏证书的 HTTPS 端点。本工作区的环境限制（代理为不稳定的 SOCKS5、本地端口必须 `NO_PROXY`）使这一类实验本身不可靠，做出来的结果也无法区分"客户端分类对了"与"代理把这个请求吃掉了"。<br>**人工核对步骤**：指向一个**存在但不监听**的端口（如 `VAULT_API=http://127.0.0.1:9`），确认出现 `unreachable` 分类而不是别的 | DSH agent | **未验证**（已进 `证据地图.md` §3） |

**加载来源的优先顺序**（越靠上越好）：

1. **真实实现** ← fixture 属于这一档（录制的真实响应原文）
2. **共享的手写替身** ← 无
3. **本次测试专用的一次性替身** ← `stubFetch` 属于这一档。**理由**：它被 6 个测试复用，
   但只在**一个文件内**；提到共享位置需要第二个使用方，而目前没有。
   它带有密集注释（每条规则都写了"为什么"），符合"只写一份、密集注释"的要求。

---

## 3. 与 `证据地图.md` §3 的一致性检查

模板要求：**没有任何"关于真实链的主张"只由替身支撑。**

| 关于真实链/真环境的主张 | 支撑它的证据 | 是否只由替身支撑 |
|---|---|---|
| 界面读数与链上数值相等 | 真浏览器 + 真 anvil `eth_call`（`browser-assert.mjs`） | **否** |
| 真实渲染下坐标不是 NaN | 真浏览器读渲染后 DOM 的 SVG 属性 | **否** |
| 服务响应的真实字段名与形状 | 线上重取（`contract-live.test.ts`、`api-against-real-service.test.ts`） | **否** |
| 页面服务端渲染返回 200 | 真实 HTTP 请求（`fetch(PAGE)`） | **否** |
| `fetch` 失败被分成三类 | 替身（`stubFetch`）+ 真实 4xx 的对照 | **否**（有对照） |
| `cache: 'no-store'` 确实被送出 | 替身捕获 `init` 对象 | **是——只由替身支撑**。已明确标注为"部分未验证"，并给出了真环境核对步骤 |
| `dynamic = 'force-dynamic'` 生效 | **无证据** | **是——连替身都没有**。已进 `证据地图.md` §3 |

> 两条只由替身支撑（或完全没有证据）的主张都在上表里点出来了，
> 没有一条被写成"已验证"。第二行更是没有替身也没有断言，只是源码里的一行声明。

---

## 4. G-F2 关卡（本文件部分）

- [x] 每条替身都写了"简化了什么"，且没有一条简化掉了被断言依赖的行为
  —— `stubFetch` 简化掉的是**真实传输**，而被断言的是 **URL 构造**与**失败分类**，
  两者不在同一个层面（见 §1 说明）。`no-store` 那一项确实被简化掉了，已标注为未验证
- [x] 判定列没有留空的"不合格"；不合格的已改或已登记为风险（无"不合格"条目）
- [x] 每条替身有对照测试，或写明了为什么不能 + 人工核对步骤（§2 三行齐备）
- [x] 示例行已全部删除（表内无 `【示例】`）
- [x] 与 `证据地图.md` §3 一致：已逐行核对，两条"只由替身支撑"的主张被点名

**G-F2 结论**：**通过（自评）** —— 2026-09-16，DSH agent。
全部替身 1 个 + 1 个"录制的真实响应"，均有对照测试或明确的未验证登记。
**不是客户验收**，理由见 `支持矩阵与验收.md` §5。

> 配套：`不变式台账.md`（要证明什么）、`证据地图.md`（证据能证明什么、不能证明什么）。
