# 支持矩阵与验收（路径 B · F5）— vault-console

> **F5 产出物。关卡 G-F5：客户按 F1 的陈述逐条确认。**
> 依据：`../../工程蓝图.md` §12.2。
>
> **本文件的第 5 节要处理一件事，先说清楚**：本工作区是**作品集**，不是客户项目，
> **没有外部客户**。模板要求"客户按 F1 的陈述逐条确认"，本项目做不到，
> 也不会假装做到。§5 把逐条陈述列成签字表，由作者以"实现者 + 验证者"的双重身份签署，
> 并在表头写明**这是自评，不是验收**。

---

## 1. 浏览器支持矩阵

**实测口径**：本项目**只在一个浏览器上实测过**（用户真实 Chrome，经 kimi-webbridge/CDP 驱动）。
其余浏览器未测。`未测` 与 `不支持` 是两件事，本表分开记。

| 浏览器 | 版本 | 是否实测 | 实测结果 | 备注 |
|---|---|---|---|---|
| Chrome（用户真实配置，含 MetaMask 13.48） | Chrome 系，版本随用户环境 | **是** | **14/14 断言通过** | 见 `docs/evidence/browser-assert.txt` 与 `console-live.png` |
| Edge | 未测 | **否** | — | 同 Chromium 内核，理论行为一致，但**没有实测就不写"支持"** |
| Firefox | 未测 | **否** | — | 未安装、未跑 |
| Safari | 未测 | **否** | — | 本机无 macOS；`preserveAspectRatio="none"` 与 SVG 行为在 WebKit 上**未验证** |
| 移动端浏览器 | 未测 | **否** | — | 响应式类名（`sm:` 断点）写入了但**未在任何窄视口实测** |

**不支持 / 明确不覆盖**

| 项 | 说明 |
|---|---|
| IE / 旧版 Edge | **不支持**：使用 `BigInt`（ES2020）、`Array.prototype.at`、`??`、`AbortSignal.timeout`，均无 polyfill |
| 无 JavaScript | **不支持**：页面依赖服务端渲染 + 客户端 hydration（图表是 `'use client'`）。服务端渲染的 HTML 里有完整的四个面板文本，但 SVG 图表需要 hydration |
| 14 个面板以上的蜡烛数 | 未验证上限。实测 169 根；`horizontalLayout` 在 5000 根时的宽度钳制有单元测试，但**渲染 5000 根的性能未测** |

---

## 2. 构建可复现性

**这里要诚实说明一个陷阱**：`next build` 的产物**包含时间戳与内容哈希化的 chunk 名**，
所以两次构建的产物哈希**本来就不相同**。拿产物哈希当"可复现性"的证据是无效的判据。
本项目用**输入哈希**：

| 输入 | 值（2026-09-16 实测） | 取法 |
|---|---|---|
| `package-lock.json` 的 sha256 | `08aedc3ad4dc7c736f503090fcb5a23ad86cb22a5e275ef6a2d19a1b678a2676` | `node tools/build-inputs.mjs` |
| `src/` 全树（路径排序 + 逐文件 sha256 折叠） | `c75133365f7d4466cd062d2e6d31fa39943433eb3652341620da194772453951`（11 个文件） | 同上 |
| `test/` + `tools/` 全树 | `41eb52c5e44b2cf9a506b6a96fed20048cf65688f4676ff1edf7db623d8263c1`（17 个文件） | 同上 |
| 四个配置文件（`next.config.ts` / `tsconfig.json` / `tailwind.config.ts` / `postcss.config.mjs`） | `93d0e7c61a3fed6e0b96c2cbc923060481069bcbafacec9ab143273848dfefb6` | 同上 |
| Node 版本 | `v24.9.0` | `node --version` |
| npm 包管理器版本 | `11.13.0`（经 `node <npm-cli.js>` 调用） | 见 `../../playbooks/环境能力清单.md` |

**两次运行是否真的做过**：**做了，且结果相同。** 相隔 2 秒连跑两次，四个哈希**逐字符一致**：

```
lockfile    08aedc3ad4dc7c736f503090fcb5a23ad86cb22a5e275ef6a2d19a1b678a2676
src         c75133365f7d4466cd062d2e6d31fa39943433eb3652341620da194772453951   (11 files)
checks      41eb52c5e44b2cf9a506b6a96fed20048cf65688f4676ff1edf7db623d8263c1   (17 files: test/ + tools/)
config      93d0e7c61a3fed6e0b96c2cbc923060481069bcbafacec9ab143273848dfefb6   (4 files)
```

**因此"构建可复现"这一条的状态是 `已验证（输入哈希层面）`**，证据在
`docs/evidence/build-inputs.txt`。

**这条证据能证明什么**：同一份源码、同一份锁文件、同一套工具链 → 同一组哈希。
**这条证据不能证明什么**：① **产物字节的确定性**——`.next/` 含时间戳与内容哈希化的
chunk 名，两次构建的产物**本来就不可能相同**，所以本检查刻意不比对产物；
② **依赖是否真的从锁文件装的**——本检查读的是 `package-lock.json` 的内容，
不是 `node_modules/` 的实际内容。若有人手改过 `node_modules`，哈希不会变。
（`npm ci` 会保证这一点，但本项目用的是 `npm install`。）

**已知的构建约束**（环境相关，不是项目缺陷）：

| 现象 | 性质 | 说明 |
|---|---|---|
| `next build` 在受限沙箱里报 `spawn EPERM`，且 `.next/BUILD_ID` 缺失 | **③ 环境限制** | Next 16 的 Turbopack 在"Collecting page data"阶段 spawn 工作进程，而受限沙箱禁止创建命名管道。**提权后同一条命令完整成功**。这**不是**代码问题，也**不是** Next 缺陷 |
| `npm test` 不能用 `node --test test/*.test.ts` | **③ 环境限制** | `node --test` 每个文件 spawn 一个子进程并**用命名管道捕获其输出**，受限沙箱下直接 `EPERM`。**换成直接执行测试文件**（测试在进程内运行、不 spawn）即可，见 `tools/run-tests.mjs` 的注释。同一条命令在沙箱外可用 |

---

## 3. 可访问性基线

**状态：`未测`。** 本项目**没有跑过任何可访问性审计工具，也没有用屏幕阅读器验证过**。
写进去的东西**不是**证据。

| 项 | 已写入的实现 | 验证状态 |
|---|---|---|
| 语义地标 | `<main>` / `<header>` / `<section>` / `<footer>` / `<dl>/<dt>/<dd>` / `<figure>/<figcaption>` | **未测**（写了，但没验证辅助技术如何呈现） |
| 图表的替代文本 | `role="img"` + `aria-label`，内容为"169 candles of 60 seconds, from 1.1 to 1.1 USDC. Latest close 1.1. The price did not move in this window." | **未测** |
| 颜色对比度 | 深色底 + `slate-100`/`slate-400`/`slate-500` 文字。`slate-500` (`#64748b`) 在 `slate-950` 上**可能不达 WCAG AA 的 4.5:1** | **未测，且有具体疑点**——这一条不是"没测"，是"按常见工具的经验值算可能不达标"。用工具测过之前不写结论 |
| 键盘可达性 | 页面**没有交互控件**（无按钮、无输入框、无链接），因此没有 tab 顺序问题；蜡烛 tooltip 是 `<title>`，**键盘与触摸都到不了** | **未测**，且 tooltip 的可达性**已知受限** |
| 减少动效偏好 | 无动画，因此 `prefers-reduced-motion` 不适用 | **不适用 + 理由** |
| 文本缩放 | 使用 Tailwind 相对单位（`text-lg` 等） | **未测** |

> 这一节**必须写点东西**，它写了七行，其中六行是 `未测`。
> 写成"已实现无障碍支持"会是本项目纪律禁止的那类记录。

---

## 4. 错误文案复核

逐条照抄实现里的文案原文，并核对它是否**指向正确的修法**。

| 场景 | 文案原文（从源码照抄） | 复核结论 |
|---|---|---|
| 链不可读 | `The chain could not be read.` + 该次失败的具体消息 | ✅ 指向"链的问题"，且附上原始消息供进一步定位 |
| 索引服务不可达 | `The index service is not reachable at <URL>. It runs as a separate process; start it with \`node --experimental-strip-types src/api/cli.ts\` in the erc4626-vault-dapp repository.` | ✅ **含实际 URL**（否则"不可达"无法区分"没起"与"起错端口"），且给出了启动命令 |
| 索引服务拒绝 | 服务自己的 `error` + `hint`，**逐字**，用 ` — ` 连接 | ✅ 不改写、不包装。服务比控制台更清楚自己为什么拒绝 |
| 索引返回非 JSON | `The index service answered, but not with JSON. That is usually a proxy answering instead of the service.` | ✅ 指名了最常见的真实原因（代理应答），而不是含糊的"格式错误" |
| **URL 根本没构造出来** | `The console built an invalid URL for the index service: <URL>. This is a bug in the console, not a problem with the service -- relative paths only resolve in a browser.` | ✅ **本项目专门加了这一条**。第一版把这种情况归入"服务不可达"，把读者送去重启一个**从头到尾都在运行**的服务 |
| 部署记录缺失 | `No deployment record found. … Tried:\n  <路径>\n  <路径>\nSet VAULT_DEPLOYMENT to point at one.` | ✅ 列出**试过哪些路径**，读者可以直接比对 |
| 部署记录字段缺失 | `The deployment record at <路径> has no "<字段>". Refusing to guess an address.` | ✅ 说明"拒绝猜"——这正是这条错误存在的理由 |
| 金额不可解析 | **原样显示该字符串**（不显示 `—`） | ✅ 刻意：显示 `—` 会**隐藏服务与控制台的数据形状分歧**，而原始字符串是调试者需要看到的东西。这一条在 `证据地图.md` 里记为"能证明什么/不能证明什么"的一行 |
| 无价格历史 | `No price history in this window.` | ✅ 与"价格没动"**文案不同**（后者是 `the price did not move in this window`）。这两件事必须可区分：一个是没数据，一个是数据很平 |
| 两个来源不齐 | `A comparison needs both sources. One of them is unavailable, so this panel does not guess.` | ✅ 明说"不猜"，而不是拿单边数据凑一个对比 |

> 全部 10 条都读了源码照抄。第 5 条是本项目**新增**的（原缺陷的直接产物）。

---

## 5. 逐条验收签字表

> ⚠️ **本表是作者自评，不是客户验收。**
> 本工作区是作品集，**没有外部客户**。模板要求"客户按 F1 的陈述逐条确认"，
> 本项目**做不到**，也不把自评写成客户签字——那会是本项目纪律禁止的记录。
> 表头写明验证方式，让读者能自己判断每条证据的分量。

| F1 编号 | 可验收陈述（摘要） | 验证方式 | 证据位置 | 状态 | 签署（人 / 日期） |
|---|---|---|---|---|---|
| UI-01 | 4 个面板都在，且无失败文案 | **真实浏览器**（渲染后 DOM） | `browser-assert.txt` → `the four panels rendered`、`no service-failure panel is showing` | **通过** | DSH agent / 2026-09-16（自评） |
| UI-02 | `totalSupply` 显示为份额，raw uint256 不在页面上 | **真实浏览器 + 真链**（期望值来自 `eth_call`） | `browser-assert.txt` → 两条 | **通过** | DSH agent / 2026-09-16（自评） |
| UI-03 | `totalAssets` 显示为 `944.9241` 而不是 `944,924,100` | 纯函数 + 浏览器 | `tests.txt`（format 组）+ `console-live.png` | **通过** | DSH agent / 2026-09-16（自评） |
| UI-04 | 两者不齐时 `Two sources` 拒绝作答 | **实现已就绪，未实测** | 源码 `page.tsx` 的 `live !== null && status !== null ? … : …` | **未跑** | — |
| UI-05 | 链不可达而 `Then` 仍工作 | **实现已就绪，未实测** | `浏览器测试计划.md` §5 第 9 行 | **未跑** | — |
| UI-06 | 索引不可达而 `Now` 仍工作 | **实现已就绪，未实测** | `浏览器测试计划.md` §5 第 10 行 | **未跑** | — |
| UI-07 | 平盘画成中部水平线，0 个 NaN 坐标 | **真实浏览器 + 纯函数** | `browser-assert.txt` → `NO NaN coordinates`；`tests.txt`（chart-geometry 组） | **通过** | DSH agent / 2026-09-16（自评） |
| UI-08 | 平盘被明确说出来 | **真实浏览器** | `browser-assert.txt` → `the page says the price did not move` | **通过** | DSH agent / 2026-09-16（自评） |
| UI-09 | 覆盖缺口原句逐字出现 | 契约（服务确实发了该字段）+ 源码逐字渲染。**浏览器断言只覆盖了"平盘"那句，没覆盖这一句** | `tests.txt`（contract 组） | **部分通过** | DSH agent / 2026-09-16（自评） |
| UI-10 | tooltip 含服务原始字符串 | **真实浏览器** | `browser-assert.txt` → `a candle tooltip carries the exact stored strings` | **通过** | DSH agent / 2026-09-16（自评） |
| UI-11 | 页脚出现只读声明 | 源码渲染 + `console-live.png` 目视 | `docs/evidence/console-live.png` | **通过**（目视，非断言） | DSH agent / 2026-09-16（自评） |
| UI-12 | 页头显示部署记录路径 | 同上 | `console-live.png` | **通过**（目视，非断言） | DSH agent / 2026-09-16（自评） |

**统计**：12 条中 **7 条通过**（其中 2 条仅目视）、**1 条部分通过**、**4 条未跑**。

> **"未跑"的 4 条全部是同一件事的四个侧面**：两个来源的独立失败。
> 它们的实现写好了、错误文案写好了、复跑步骤写好了，但**一次也没真的停服务跑过**。
> 这是本项目最重要的证据缺口，在 `证据地图.md` §3、`不变式台账.md` INV-03、
> `浏览器测试计划.md` §8 里被**同一件事四处登记**——重复登记是刻意的，
> 免得读者只在某一处看到它。

---

## 6. 遗留事项与风险接受

| # | 事项 | 为什么没做 | 风险 | 接受人 |
|---|---|---|---|---|
| 1 | **两个来源独立失败的实测**（停服务复跑，覆盖 UI-04/05/06） | 需要单独一轮：停服务 → 跑失败场景 → 重启 → 重跑全套断言。在与 L1/线上契约/浏览器断言共用同一批活服务的会话里做不到 | 若独立性其实不成立（例如某个 `await` 意外让它变成串行），页面在单边故障时的行为**可能**与设计不符。**读代码判断它成立，但那是阅读，不是证据** | DSH agent |
| 2 | **构建可复现性**（两次构建比对输入哈希） | 只跑过一次 `next build` | 低。`package-lock.json` 已提交，输入哈希可取 | DSH agent |
| 3 | **可访问性基线** | 未装审计工具，未用屏幕阅读器 | 中。`slate-500` 在深色底上的对比度**可能不达 AA**；tooltip 键盘不可达 | DSH agent |
| 4 | **只有真实浏览器 console 的间接检查** | kimi-webbridge 的 `evaluate` 不能在页面上下文回溯已发生的 console 输出 | 低——用"页面文本不含错误与 hydration 提示"替代。但**未捕获的异常若不留文本痕迹就查不到** | DSH agent |
| 5 | **非平盘价格数据集** | 本机库的价格全程 `1.1`（每次 yield report 同比例抬高 `totalAssets` 且不铸币） | 低——非平盘路径由合成蜡烛的单元测试覆盖。但**没有真实数据走过那条路** | DSH agent |
| 6 | **`dynamic = 'force-dynamic'` 生效** | 未写成断言 | 低——源码上是显式导出。但"写了"不是"生效了" | DSH agent |
| 7 | **覆盖缺口是否不可避免** | 未换 archive 节点复核服务的说法 | 低——但这是**依赖服务的自我陈述**，本项目没有独立验证 | DSH agent |
| 8 | **超时 / 429 限流 / TLS 错误** | 本机环境（不稳定的 SOCKS5 代理、本地端口必须 `NO_PROXY`）使这类实验不可靠 | 中——只有"服务进程不在"这一种不可用形态被测过 | DSH agent |

> 全部 8 项由 DSH agent 以**作品集作者**身份接受。
> **没有客户书面接受**——因为没有客户。这是本文件与模板最大的差异，写在最显眼的位置。

---

## 7. G-F5 关卡

- [x] 浏览器支持矩阵：`未测` 与 `不支持` 分开（§1），实测的只有一个浏览器并写明
- [x] 构建可复现性：**判据已选对**（输入哈希而非产物哈希），但**两次构建未做** → 状态 `未验证`
- [x] 可访问性基线：§3 如实写 `未测`，并点出对比度与 tooltip 两个**具体疑点**
- [x] 错误文案复核：10 条照抄源码原文（§4）
- [ ] **客户按 F1 的陈述逐条确认** —— **无法满足**：无外部客户。§5 由作者自评并明确标注性质
- [x] 遗留事项与风险接受：8 项，逐项写清"为什么没做"与接受人（§6）

**G-F5 结论**：**有条件通过（自评，且含明确未跑项）** —— 2026-09-16，DSH agent。

**条件**（三条，缺一条则 F5 不算通过）：
1. §5 的 4 条 `未跑` 必须在补跑后改为通过或明确的不适用；
2. §2 的构建可复现性必须真的跑两次并记下两个输入哈希；
3. §3 的可访问性必须用工具实测一次，至少回答"`slate-500` 在 `slate-950` 上是否达 AA"。

**关于"客户确认"这一项**：模板把它列为 G-F5 的判据之一，本工作区**结构上无法满足**。
按蓝图 §12.1 的纪律（"不适用的也要写出来并给理由，**空白不算声明**"），
它在这里被显式标为无法满足并给出理由，而不是留空、也不是伪造一个签字。

> 这一步之后的动作不在本文件范围内：蓝图 §12.2 的路径 B 在 F5 之后进入
> 路径 D 的 I1–I5（集成/部署）。本项目的 I 阶段状态见 `浏览器测试计划.md` §6。
